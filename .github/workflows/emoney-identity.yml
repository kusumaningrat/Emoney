name: EMoney Identity

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      secret_key:
        description: "Enter pipeline key to proceed"
        required: true
        type: string
      environment:
        description: "Environment to deploy"
        required: true
        type: choice
        options:
          - staging
          - prod
      sha:
        description: "SHA to promote (prod only)"
        required: false
        type: string

env:
  TARGET_ENV: ${{ github.event_name == 'push' && 'dev' || inputs.environment }}

jobs:
  # ─────────────────────────────────────────
  # KEY VALIDATION (manual trigger only)
  # ─────────────────────────────────────────
  validate-key:
    name: Validate Pipeline Key
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Check pipeline key
        env:
          PIPELINE_KEY: ${{ secrets.PIPELINE_KEY }}
          INPUT_KEY: ${{ github.event.inputs.secret_key }}
        run: |
          if [ "$INPUT_KEY" != "$PIPELINE_KEY" ]; then
            echo "❌ Invalid pipeline key. Access denied."
            exit 1
          fi
          echo "✅ Key validated. Proceeding..."

  # ─────────────────────────────────────────
  # BUILD (auto on push, or manual staging)
  # ─────────────────────────────────────────
  build:
    needs: [validate-key]
    # validate-key is skipped on push, so we handle both cases:
    if: |
      always() &&
      (
        github.event_name == 'push' ||
        (
          github.event_name == 'workflow_dispatch' &&
          inputs.environment == 'staging' &&
          needs.validate-key.result == 'success'
        )
      )
    uses: ./.github/workflows/_reusable-build.yml
    with:
      service-dir: eMoney-identity
      service-name: emoney-identity
      image-base: image-reg.ops.glynac.ai/emoney-advisor
      environment: ${{ github.event_name == 'push' && 'dev' || inputs.environment }}
    secrets:
      HARBOR_TOKEN: ${{ secrets.HARBOR_TOKEN }}

  # ─────────────────────────────────────────
  # DEPLOY DEV — auto on every push to main
  # ─────────────────────────────────────────
  deploy-dev:
    needs: build
    if: |
      always() &&
      github.event_name == 'push' &&
      needs.build.result == 'success'
    uses: ./.github/workflows/_reusable-deploy.yml
    with:
      environment: dev
      image-tag: ${{ needs.build.outputs.image-tag }}
      nomad-addr: ${{ vars.NOMAD_ADDR }}
      nomad-job-file: nomad/emoney-identity-dev.hcl
    secrets:
      HARBOR_TOKEN: ${{ secrets.HARBOR_TOKEN }}
      NOMAD_TOKEN: ${{ secrets.NOMAD_TOKEN }}
      TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}

  # ─────────────────────────────────────────
  # DEPLOY STAGING — manual only
  # ─────────────────────────────────────────
  deploy-staging:
    needs: [validate-key, build]
    if: |
      always() &&
      github.event_name == 'workflow_dispatch' &&
      inputs.environment == 'staging' &&
      needs.validate-key.result == 'success' &&
      needs.build.result == 'success'
    uses: ./.github/workflows/_reusable-deploy.yml
    with:
      environment: staging
      image-tag: ${{ needs.build.outputs.image-tag }}
      nomad-addr: ${{ vars.NOMAD_ADDR_STAGING }}
      nomad-job-file: nomad/emoney-identity-staging.hcl
    secrets:
      HARBOR_TOKEN: ${{ secrets.HARBOR_TOKEN }}
      NOMAD_TOKEN: ${{ secrets.NOMAD_TOKEN }}
      TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}

  # ─────────────────────────────────────────
  # DEPLOY PROD — manual only, requires SHA
  # ─────────────────────────────────────────
  deploy-prod:
    needs: [validate-key]
    if: |
      always() &&
      github.event_name == 'workflow_dispatch' &&
      inputs.environment == 'prod' &&
      needs.validate-key.result == 'success'
    uses: ./.github/workflows/_reusable-deploy.yml
    with:
      environment: prod
      image-tag: staging-${{ inputs.sha }}
      nomad-addr: ${{ vars.NOMAD_ADDR }}
      nomad-job-file: nomad/emoney-identity-prod.hcl
    secrets:
      HARBOR_TOKEN: ${{ secrets.HARBOR_TOKEN }}
      NOMAD_TOKEN: ${{ secrets.NOMAD_TOKEN }}
      TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
